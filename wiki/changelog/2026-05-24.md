# Changelog - 2026-05-24

## Session Notes
- Session initialized.

## Encoding and Wiki Gate Hardening
- Normalized UTF-8 and line-ending rules through `.gitattributes` and `.editorconfig`.
- Added explicit Python UTF-8 environment settings to CI/wiki-memory workflows.
- Updated touched domain docs for `admin-governance`, `onboarding-saas`, and `quote-approval` so the domain coverage gate remains above the bootstrap threshold.

## Guard Verification Notes
- CODEOWNERS uses the GitHub mention format `@olimposbotsinyal`; `gh` is not installed locally, so owner existence requires manual verification.
- `tools/agent/SESSION_CONTEXT.md` is tracked and expected to remain not ignored.
- Pre-commit delete guard remains active; branch protection should require PR review and Code Owner review before merge.

## Security Follow-up
- `scripts/deploy_to_plesk.ps1` still contains a hardcoded deploy credential for compatibility with the current manual script.
- Follow-up: move this secret to an environment variable or secret manager, update the script to read from that source, and rotate the exposed credential after rollout.
- This follow-up should be non-destructive and staged so the current deploy path is not broken unexpectedly.
